// GLE1 for CSCE312

/**
 * Nor gate:
 * if (a or b) out = 0, else out = 1
 */

//// This chip is already implemented!
CHIP Nor {
    IN a, b;
    OUT out;
    
    PARTS:
    Nor(a=a, b=false, out=out1);
    Nor(a=false, b=b, out=out2);
    Nor(a=out1, b=out2, out=out);
}