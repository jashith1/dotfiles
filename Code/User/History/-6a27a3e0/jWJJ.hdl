// GLE1 for CSCE312
// NAME and UIN

/**
 * Nand gate:
 * if (a and b) out = 0, else out = 1
 */

CHIP Nand {
    IN a, b;
    OUT out;
    
    PARTS:
    //// Task: Build a NAND chip using only NOR gates.
    Nor(a=a, b=a, out=out);
}